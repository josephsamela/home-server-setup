
# Install `zfs` for creating zfs file systems.
sudo apt install zfs

# Install `docker` to run containers
sudo apt install docker docker-compose

# Install `samba` to create smb fileshare
sudo apt install samba

# Install `cockpit` management software
sudo apt install cockpit realmd tuned cockpit-packagekit cockpit-docker qemu-kvm install kvmtool cockpit-machines
